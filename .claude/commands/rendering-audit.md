# /rendering-audit - Next.js 렌더링 전략 감사

너는 Next.js 퍼포먼스 아키텍트이자 웹 렌더링 최적화 전문가야. 이 프로젝트의 `src/app/` 디렉토리 내 모든 page.tsx, layout.tsx 파일을 분석해서 현재 사용 중인 렌더링 방식이 효율적인지 판단하고, 최적의 렌더링 전략을 제안하는 리포트를 작성해 줘.

## 분석 기준

1. **Server Component 우선**: 가능한 한 클라이언트 컴포넌트(`'use client'`)를 줄이고 서버 컴포넌트(RSC)로 전환 가능한지 확인.

2. **데이터 성격 분류**:
   - 정적 데이터(변경 빈도 낮음) → SSG (`generateStaticParams`)
   - 주기적 갱신 데이터(CMS, 블로그 등) → ISR (`revalidate`)
   - 실시간/개인화 데이터 → RSC + Suspense (Streaming)
   - 정적 껍데기 + 동적 알맹이 혼합 → PPR (Partial Prerendering)

3. **CSR 남용 탐지**: `useEffect`로 초기 데이터를 가져오는 패턴을 찾아내어 서버 사이드 페칭으로 변경 제안.

## 분석 대상
- `src/app/**/page.tsx` — 모든 페이지 컴포넌트
- `src/app/**/layout.tsx` — 레이아웃 컴포넌트
- `src/presentation/components/**` — 페이지에서 import하는 주요 컴포넌트의 `'use client'` 여부

## 리포트 포맷

| 파일 경로 (app/...) | 현재 방식 (추정) | 권장 전략 | 예상 임팩트 | 핵심 변경 사항 |
|:---|:---|:---|:---|:---|
| 예시 | 🔴 CSR / 🟡 SSR / 🟢 ISR / 🔵 SSG | 🔵 SSG / 🟢 ISR / 🟠 RSC / 🟣 PPR | TTFB, LCP, SEO 등 | 구체적 코드 변경 |

## 추가 출력
1. **우선순위 TOP 5**: 가장 임팩트가 큰 변경 5개를 우선순위로 정리
2. **'use client' 제거 후보**: RSC로 전환 가능한 클라이언트 컴포넌트 목록
3. **번들 사이즈 절감 예상**: 클라이언트 번들에서 제거 가능한 주요 의존성
